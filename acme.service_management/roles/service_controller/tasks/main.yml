---
- name: Ensure service is in desired state
  ansible.builtin.service:
    name: "{{ service_name }}"
    state: "{{ service_state }}"
    enabled: "{{ service_enabled }}"
  tags:
    - service
    - service_controller
