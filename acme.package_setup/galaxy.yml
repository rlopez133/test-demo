---
# Ansible Galaxy collection metadata
namespace: acme
name: package_setup
version: 1.0.0
readme: README.md
description: >
  Installs and manages the nginx package and ensures the nginx service
  is running and enabled on target hosts.
author:
  - acme
license:
  - GPL-2.0-or-later
tags:
  - nginx
  - package
  - service
repository: https://github.com/acme/package_setup
documentation: https://github.com/acme/package_setup
homepage: https://github.com/acme/package_setup
dependencies: {}
