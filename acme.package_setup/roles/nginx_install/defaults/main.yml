---
# Package management variables for Nginx installation

# nginx_package_name: Name of the Nginx package to install
nginx_package_name: nginx

# nginx_package_state: Desired state of the Nginx package
# Valid values: present, absent, latest
nginx_package_state: present
