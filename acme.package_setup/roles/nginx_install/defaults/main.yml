---
# Nginx installation defaults

# Package name for nginx (may vary by distribution)
nginx_package_name: nginx

# Desired state of the nginx package (present/absent/latest)
nginx_package_state: present

# Service name for nginx
nginx_service_name: nginx
