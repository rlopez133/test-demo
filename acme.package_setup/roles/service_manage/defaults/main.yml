---
# Default variables for service management role

# Service name to manage
service_name: nginx

# Service action: started, stopped, restarted, reloaded
service_state: started

# Enable service on boot
service_enabled: true

# Daemon reload before starting service (systemd)
daemon_reload: false