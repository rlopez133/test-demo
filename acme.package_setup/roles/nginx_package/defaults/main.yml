---
# Nginx Package Installation Defaults
# This role installs the nginx package on supported systems

# Package name (can be overridden if alternative package source is used)
nginx_package_name: nginx

# Package state: present (installed) or absent (uninstalled)
nginx_package_state: present

# Update package cache before installation (recommended for fresh systems)
nginx_update_cache: true
