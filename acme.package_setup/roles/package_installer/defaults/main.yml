---
# Package installer default variables
# These variables control which packages are installed and their desired state

# Name of the package to install
package_name: nginx

# Desired state of the package: 'present' to install, 'absent' to remove
package_state: present
