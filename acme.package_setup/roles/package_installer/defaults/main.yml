---
# Package name to install
package_name: nginx

# Desired state of the package (present, absent, latest)
package_state: present

# Update package cache before installation
update_package_cache: false
