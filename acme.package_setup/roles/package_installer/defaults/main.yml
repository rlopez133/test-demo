---
# Package installer role default variables

# Name of the package to install
package_name: nginx

# Desired state of the package (present, absent, latest)
package_state: present

# Update package cache before installing (recommended for fresh environments)
package_update_cache: true

# Cache update timeout in seconds
package_cache_timeout: 3600
