---
# Default variables for package_installer role

# List of packages to install
package_installer_packages:
  - nginx

# Package state: present, absent, latest
package_installer_state: present

# Update package cache before installation
package_installer_update_cache: true

# Cache update timeout in seconds
package_installer_cache_timeout: 3600