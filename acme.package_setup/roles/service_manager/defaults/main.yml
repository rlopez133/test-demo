---
# Service manager role default variables

# Name of the service to manage
service_name: nginx

# Desired action for the service (started, stopped, restarted, reloaded)
service_action: started

# Whether the service should be enabled at boot
service_enabled: true

# Whether to wait for the service to reach desired state (in seconds)
service_timeout: 30
