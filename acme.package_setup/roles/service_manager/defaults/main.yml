---
# Service manager role defaults
# These variables control service state management

# Service name to manage
service_name: nginx

# Service state: 'started', 'stopped', 'restarted', 'reloaded'
service_state: started

# Whether to enable service on boot
service_enabled: true

# Daemon reload before managing service (systemd systems)
daemon_reload: false
