---
# Default variables for service_manager role

# List of services to manage
service_manager_services:
  - nginx

# Service state: started, stopped, restarted, reloaded
service_manager_state: started

# Whether service should start on boot
service_manager_enabled: true

# Daemon reload before managing services (for systemd)
service_manager_daemon_reload: false