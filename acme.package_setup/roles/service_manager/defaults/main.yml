---
# Service manager default variables
# These variables control service state and startup behavior

# Name of the service to manage
service_name: nginx

# Desired service action: 'started', 'stopped', 'restarted', or 'reloaded'
service_action: started

# Whether the service should be enabled (auto-start on boot)
service_enabled: true
