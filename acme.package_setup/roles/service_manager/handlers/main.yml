---
# Handlers for service_manager role

- name: Restart managed services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
    daemon_reload: "{{ service_manager_daemon_reload }}"
  loop: "{{ service_manager_services }}"
  listen: restart services
  tags:
    - service_manager
    - service