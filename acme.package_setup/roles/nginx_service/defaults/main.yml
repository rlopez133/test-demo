---
# Nginx service management defaults

# Service name for nginx
nginx_service_name: nginx

# Desired state of the nginx service (started/stopped/restarted/reloaded)
nginx_service_state: started

# Whether to enable nginx on system boot
nginx_service_enabled: yes
