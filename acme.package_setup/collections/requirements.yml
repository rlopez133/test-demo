---
# External collection dependencies for ACME Package Setup collection
# All required collections are listed here with their minimum versions

collections:
  # No external collections required - all modules are from ansible.builtin
