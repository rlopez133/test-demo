---
# External collection dependencies
# This collection uses only Ansible built-in modules and does not require
# any external collections beyond ansible.builtin (included with Ansible Core)
collections: []
