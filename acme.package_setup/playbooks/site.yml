---
- name: acme.package_setup | Main Playbook - Package and Service Management
  hosts: all
  gather_facts: true
  tasks:
    - name: Include nginx_setup role
      ansible.builtin.include_role:
        name: nginx_setup
      tags:
        - nginx_setup
        - packages
        - services